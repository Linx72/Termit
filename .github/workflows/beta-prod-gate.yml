name: Beta Prod Gate

# Еженедельная проверка prod beta D30 (нужен secret TERMIT_BETA_PROD_URL).
on:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:

jobs:
  beta-prod-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install -q -r requirements.txt

      # secrets нельзя в if: — проверка URL внутри step через env.
      - name: Beta prod gate
        env:
          TERMIT_BETA_PROD_URL: ${{ secrets.TERMIT_BETA_PROD_URL }}
          TERMIT_API_KEY: ${{ secrets.TERMIT_API_KEY }}
        run: |
          if [ -z "${TERMIT_BETA_PROD_URL}" ]; then
            echo "TERMIT_BETA_PROD_URL not configured — skip prod beta gate."
            exit 0
          fi
          ./scripts/beta_prod_gate.sh

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: beta-prod-gate
          path: data/beta_prod_gate_last.json
          if-no-files-found: ignore
