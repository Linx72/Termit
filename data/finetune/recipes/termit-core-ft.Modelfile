# Baseline Termit core adapter (Stage1 placeholder until LoRA/GGUF finetune).
# Base: 14B coder для стабильного tool loop (фаза A model ladder).
# Пересоздать: ./scripts/upgrade_model_ladder_phase_a.sh или scripts/setup_stage1_adapter.sh
FROM qwen2.5-coder:14b
PARAMETER temperature 0.2
SYSTEM You are a domain-specific coding assistant for the Termit Python orchestrator (app/, agents, platform API). Prefer minimal diffs, existing patterns, structured tool JSON when asked, and run relevant tests yourself.
